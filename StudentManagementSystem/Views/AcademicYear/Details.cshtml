@model StudentManagementSystem.PL.ViewModel.AcademicYear.GetAcademicYearVm

@{
    ViewData["Title"] = "Academic Year Details";
}

<h2>Academic Year Details</h2>

<div class="card p-3">
    <h4>@Model.Name</h4>
</div>

<a asp-action="GetAll" class="btn btn-secondary mt-3">Back</a>
